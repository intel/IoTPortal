const timerActionTypes = {
  // Tick current time
  TICK_CURRENT_TIME_START: 'TICK_CURRENT_TIME_START',
  TICK_CURRENT_TIME_STOP: 'TICK_CURRENT_TIME_STOP',
  UPDATE_CURRENT_TIME: 'UPDATE_CURRENT_TIME',
};

export default timerActionTypes;